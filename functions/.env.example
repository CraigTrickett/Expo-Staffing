# Only needed if Firestore was provisioned under a named database rather
# than the implicit "(default)" one (this is how AI Studio provisions it
# by default). Must match VITE_FIREBASE_DATABASE_ID used by the frontend
# (see the root .env.example). Leave blank/unset if your project uses the
# default database.
FIRESTORE_DATABASE_ID=
